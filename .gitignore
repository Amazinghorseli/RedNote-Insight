# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/

# 环境变量
.env

# 临时文件
sample_data.csv
*_test.csv

# 运行时日志
app.log
server.log
*.log

# Vector DB index files are small — we COMMIT them so Streamlit Cloud
# doesn't have to rebuild from scratch (saves 30-60s cold start).
# Only ignore if it gets too large (>50MB).

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Streamlit
# .streamlit/secrets.toml — 提交此文件以在免费版 Streamlit Cloud 使用

# 小红书登录 Cookie（敏感信息，不要提交）
data/cookies.json
.streamlit/secrets.toml

# 运行时缓存
data/trending_cache.json
data/trending_cache*.json
