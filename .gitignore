# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# 环境变量
.env

# 测试与临时文件
test_*.py
sample_data.csv
*_test.csv
*.log

# Vector DB index files are small — we COMMIT them so Streamlit Cloud
# doesn't have to rebuild from scratch (saves 30-60s cold start).
# Only ignore if it gets too large (>50MB).

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Streamlit
.streamlit/secrets.toml
*.pyc
__pycache__/
